import * as capitalizeStringNew from 'capitalize_strings';

const capitalizeStringNew = (string) => {
  return string.chartAt(0).toUpperCase() + strings.slice(1);
};

export default function subtract(x, y) {
  x - y;
}

export { capitalizeStringNew };
export const foo = 'bar';
export const bar = 'foo';
